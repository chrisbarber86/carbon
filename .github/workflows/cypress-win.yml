name: Cypress tests on windows-latest all browsers

on: [push, pull_request]
  # schedule:
    # every day
    # - cron:  '0 0 * * *'

jobs:
  parallel-runs-across-browsers:
    name: Windows - ${{ matrix.browser }}
    strategy:
      # when one test fails, DO NOT cancel the other
      # containers, because this will kill Cypress processes
      # leaving the Dashboard hanging ...
      # https://github.com/cypress-io/github-action/issues/48
      fail-fast: false
      matrix:
        # run 7 copies of the current job in parallel
        # and they will load balance all specs
        browser: ['chrome', 'firefox', 'edge']
        machines: [ 1, 2, 3, 4, 5, 6, 7 ]
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      # because of "record" and "parallel" parameters
      # these containers will load balance all found tests among themselves
      - name: Cypress run
        uses: cypress-io/github-action@v1
        timeout-minutes: 5
        with:
          start: npm start
          # quote the url to be safe against YML parsing surprises
          wait-on: 'http://localhost:9001'
          wait-on-timeout: 300
          record: true
          parallel: true
          group: windows-${{ matrix.browser }}
          browser: ${{ matrix.browser }}
        env:
          # pass the Dashboard record key as an environment variable
          CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
          # Recommended: pass the GitHub token lets this action correctly
          # determine the unique run id necessary to re-run the checks
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}